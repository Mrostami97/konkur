# نقشهٔ اجرای محتوایی و محصولی کنکورصفریک

> این فایل مرجع اجرایی برنامهٔ چندفازی kunkur01 است. هر مورد فقط پس از پیاده‌سازی، آزمون و بازبینی تیک می‌خورد.

- آخرین به‌روزرسانی: ۱۴۰۵/۰۶/۲۵
- فاز فعال: فاز ۱۸ — پولیش، انتشار و سنجش
- commit مرتبط: `c46243d` نقطهٔ شروع فاز ۱۸ روی `main`؛ commit نهایی پس از تکمیل Gate ثبت می‌شود
- وضعیت آزمون‌ها: تست مستقل HTTP routing فاز ۱۸ محلی PASS؛ مجموعهٔ کامل CI، SSR/SEO، استقرار و بازبینی دستی هنوز باید روی نسخهٔ نهایی اجرا و ثبت شود
- قانون اجرا: هر فاز Gate، commit و push مستقل دارد؛ ادامهٔ فاز بعد فقط پس از بسته‌شدن فاز جاری انجام می‌شود.
- قانون انتشار: صفحات محتوایی تا زمان بازبینی واقعی کاربر Draft می‌مانند.
- قانون انتساب: نام «محمد رستمی» فقط پس از بازبینی واقعی او به‌عنوان نویسنده یا بازبین نمایش داده می‌شود.
- قانون رقابتی: محتوای رقبا کپی، scrape یا بازنویسی سطحی نمی‌شود؛ تحلیل فقط برای کشف نیاز کاربر است.
- قانون ابزار: Codebase Memory در این پروژه استفاده یا دوباره اضافه نمی‌شود.

## فاز ۸ — تحقیق رقابتی و نظام تحریریه

- [x] `P08-01` ساخت `TODO.md` با تمام فازها، شناسه‌ها، وضعیت فاز، commit و آزمون‌ها.
- [x] `P08-02` ثبت snapshot منبع‌دار امکانات، پوشش و محدودیت رقبا.
- [x] `P08-03` ساخت ماتریس «نیاز کاربر ← پوشش رقبا ← پاسخ اصیل kunkur01».
- [x] `P08-04` تعیین intent، کلیدواژهٔ اصلی، مخاطب و CTA برای هر صفحهٔ لانچ.
- [x] `P08-05` تدوین راهنمای لحن فارسی طبیعی و قواعد حذف کلیشه، تکرار، اغراق و keyword stuffing.
- [x] `P08-06` تدوین سیاست منبع، کپی‌رایت، ادعا، رضایت کارنامه و اصلاح خطا.
- [x] `P08-07` تهیهٔ brief مستقل برای ۶۰ صفحهٔ ارشد.
- [x] `P08-GATE` تأیید کامل ماتریس، منابع، لحن و فهرست صفحات؛ اجرای کنترل‌های کیفیت اسناد.

### گزارش Gate فاز ۸

- قرارداد سندی: PASS؛ دقیقاً ۶۰ brief با ۶۰ شناسه و ۶۰ slug یکتا و ۱۴ فیلد کامل برای هر صفحه.
- توزیع صفحات: ۳ ستون رشته، ۱۷ هاب درس، ۱۲ راهنمای مطالعه، ۱۰ صفحهٔ رسمی، ۸ راهنمای تصمیم، ۶ مطالعهٔ موردی و ۴ صفحهٔ اعتماد.
- ماتریس محصول: ۲۱ نیاز مستقل با شاهد رقابتی، شکاف، پاسخ اصیل، معیار موفقیت و فاز مقصد.
- منابع: ۲۵ URL یکتا بررسی شد؛ ۲۰ پاسخ مستقیم ۲۰۰ داشت. محدودیت واکشی Telegram، Google و صفحهٔ اصلی KonkurComputer در snapshot ثبت یا با ابزار پژوهش/نتیجهٔ ایندکس‌شده کنترل شد و برای مقایسهٔ عمومی نیازمند بازبینی انسانی دوباره است.
- کیفیت فایل: trailing whitespace صفر؛ همهٔ briefها Draft و انتساب انسانی تا بازبینی واقعی مسدود است.
- آزمون کد: اجرا نشد، چون این فاز فقط اسناد تحقیق و تحریریه را تغییر داد و هیچ کد، قرارداد runtime یا migration تغییر نکرد.

## فاز ۹ — مدل محتوا، قراردادها و پنل تحریریه

- [x] `P09-01` افزودن additive مدل‌های `ContentSource`، `Resource` و `ContributorProfile`.
- [x] `P09-02` توسعهٔ `Article` با نوع محتوا، پاسخ سریع، SEO، مقطع، رشته، سال اعتبار و نویسنده/بازبین.
- [x] `P09-03` توسعهٔ `Subject` و `Topic` با slug، توضیح، ترتیب، پیش‌نیاز و metadata.
- [x] `P09-04` افزودن `publicConsent=false` به `ReportCard`.
- [x] `P09-05` تعریف `article.v2` و حفظ کامل پشتیبانی `article.v1`.
- [x] `P09-06` ارتقای پنل ادمین برای منبع، نویسنده، بازبین، SEO و Draft → Review → Publish.
- [x] `P09-07` انتقال محتوای فعلی به مسیر نسخه‌دار بدون تغییر slug یا canonical.
- [x] `P09-08` ساخت migration استاندارد additive با up/down و آزمون حفظ داده.
- [x] `P09-GATE` موفقیت قراردادها، migration دوطرفه، مجوزها و چرخهٔ انتشار.

### گزارش Gate فاز ۹

- مدل و دسترسی: دوره و منبع از `PUBLIC`، `ACCOUNT` و `ENTITLEMENT` پشتیبانی می‌کنند؛ خرید واقعی Productهای دوره، جزوه و Bundle، مجوزهای چندگانه، زمان شروع/انقضا و لغو آزموده شد.
- تحریریه: قرارداد `article.v1` دست‌نخورده ماند و `article.v2`، منبع، taxonomy، SEO، نویسنده و چرخهٔ Draft → Review → Publish → Revision را اضافه کرد.
- امنیت انتشار: فایل Resource فقط با روش میزبانی و حقوق مستند مجاز، از endpoint کنترل‌شده و به‌صورت inline تحویل می‌شود؛ checksum فایل‌های جاری، جداشده و تاریخی Resource عمومی نیست.
- انتقال: ۱۴ صفحهٔ static با slug ثابت و ۳ منبع، deterministic و idempotent در وضعیت Draft آماده شدند. seed تراکنشی است و هیچ وضعیت یا ویرایش انسانی موجود را بازنویسی نمی‌کند.
- migration: ده migration قبلی همراه دادهٔ legacy اعمال شد؛ up، down، guard دادهٔ جدید و atomicity همگی PASS بودند. Prisma schema معتبر، ۱۱ migration به‌روز و schema diff خالی است.
- آزمون‌ها: ۹ تست قرارداد، ۱۵ تست واحد API و ۶۷ تست E2E پاس شدند؛ typecheck، lint و build کل workspace نیز موفق بود و Next.js هر ۷۱ مسیر را ساخت.
- رابط کاربری: پنل‌های مقاله، منبع، مشارکت‌کننده و Resource با Componentها و Design System موجود ساخته شدند؛ هیچ فایل CSS عمومی یا بازطراحی سراسری تغییر نکرد.
- محدوده: مسیرهای عمومی و canonical فعلی حفظ شدند، فاز ۱۰ آغاز نشد و Codebase Memory در هیچ بخش فاز ۹ استفاده نشد.

## فاز ۱۰ — معماری عمومی محتوا و SEO

- [x] `P10-01` اتصال `/guides`، `/articles` و `/subjects` به محتوای canonical منتشرشده.
- [x] `P10-02` افزودن `/topics/[slug]`، `/resources/[slug]`، `/authors/[slug]`، `/report-cards` و `/search`.
- [x] `P10-03` ساخت قالب مشترک پاسخ سریع، فهرست مطالب، منبع، تاریخ بررسی، نویسنده، بازبین و مطالب مرتبط.
- [x] `P10-04` ساخت جست‌وجوی فارسی با یکسان‌سازی ی/ک، نیم‌فاصله و وزن بیشتر عنوان نسبت به متن.
- [x] `P10-05` ساخت sitemap index مجزا برای راهنما، مقاله، درس، مبحث، منبع و کارنامه.
- [x] `P10-06` canonical و noindex برای فیلترها، pagination و URLهای تکراری.
- [x] `P10-07` افزودن JSON-LD معتبر و غیرگمراه‌کننده برای Article، Breadcrumb، Person، Course و Dataset.
- [x] `P10-08` تکمیل RSS، Open Graph، robots و مسیر اصلاح محتوا.
- [x] `P10-GATE` SSR کامل، HTML معنایی، RTL و SEO/Accessibility حداقل ۹۵.

### گزارش Gate فاز ۱۰

- معماری عمومی: راهنماها، مقاله‌ها و درس‌ها محتوای canonical منتشرشده را از API می‌خوانند و محتوای پایهٔ معتبر فعلی را هنگام نبود رکورد منتشرشده حفظ می‌کنند؛ مسیرهای مبحث، منبع، نویسنده، کارنامه و جست‌وجو نیز به‌صورت SSR اضافه شدند.
- حریم خصوصی و دسترسی: کارنامهٔ عمومی فقط با رضایت صریح و به‌شکل ناشناس خروجی می‌گیرد؛ جزئیات داخلی منبع و کلید ذخیره‌سازی افشا نمی‌شود و دسترسی Resource پولی در سمت سرور کنترل می‌شود.
- SEO: sitemap index شامل هفت نقشهٔ مجزا، RSS، robots، `llms.txt`، Open Graph، canonical، noindex صفحات خصوصی/تکراری و JSON-LDهای Article، Breadcrumb، Person، Course و Dataset تکمیل شد.
- جست‌وجو: یکسان‌سازی حروف فارسی/عربی، نیم‌فاصله و فاصله همراه با رتبه‌دهی قطعی و وزن بیشتر عنوان پیاده‌سازی و با API واقعی آزموده شد.
- آزمون‌ها: ۹ تست قرارداد، ۱۵ تست واحد API، ۷۲ تست E2E و ۵ تست اختصاصی کشف محتوای فاز ۱۰ پاس شدند؛ typecheck، lint، build کل workspace و smoke واقعی مسیرهای عمومی نیز موفق بود و Next.js هر ۸۰ مسیر را ساخت.
- سنجش مستقل: Lighthouse روی راهنمای عمومی ارشد مهندسی کامپیوتر ۱۴۰۶، امتیاز SEO برابر ۱۰۰ و Accessibility برابر ۹۶ ثبت کرد؛ تست رگرسیون اختصاصی SEO نیز PASS شد.
- محدوده: migration یا CSS عمومی جدیدی اضافه نشد، پنل ادمین باز نشد، فاز ۱۱ آغاز نشد و Codebase Memory در هیچ بخش فاز ۱۰ استفاده نشد.

## فاز ۱۱ — ۲۰ صفحهٔ ستون و درس‌های ارشد

- [x] `P11-01` راهنمای ستون ارشد مهندسی و علم کامپیوتر ۱۴۰۶.
- [x] `P11-02` راهنمای ستون ارشد مهندسی فناوری اطلاعات ۱۴۰۶.
- [x] `P11-03` راهنمای ستون ارشد علوم کامپیوتر ۱۴۰۶.
- [x] `P11-04` هاب درس زبان عمومی و تخصصی.
- [x] `P11-05` هاب ریاضیات گسسته.
- [x] `P11-06` هاب آمار و احتمال.
- [x] `P11-07` هاب جبر خطی.
- [x] `P11-08` هاب مبانی برنامه‌سازی.
- [x] `P11-09` هاب مدارهای منطقی.
- [x] `P11-10` هاب معماری کامپیوتر.
- [x] `P11-11` هاب داده‌ساختارها و الگوریتم‌های یکپارچه.
- [x] `P11-12` هاب ساختمان داده‌های مستقل.
- [x] `P11-13` هاب طراحی الگوریتم‌های مستقل.
- [x] `P11-14` هاب نظریهٔ زبان‌ها و ماشین‌ها.
- [x] `P11-15` هاب سیستم‌عامل.
- [x] `P11-16` هاب هوش مصنوعی.
- [x] `P11-17` هاب شبکه‌های کامپیوتری.
- [x] `P11-18` هاب طراحی پایگاه داده‌ها.
- [x] `P11-19` هاب مهندسی نرم‌افزار.
- [x] `P11-20` هاب اصول و خدمات فناوری اطلاعات.
- [x] `P11-GATE` تأیید منبع، وضعیت ۱۴۰۶، نمایش موبایل و لینک‌سازی هر ۲۰ صفحه.

### گزارش Gate فاز ۱۱

- صفحات: سه راهنمای ستون مجموعه‌های ۱۲۷۷، ۱۲۷۶ و ۱۲۰۹ و هفده هاب درس با پاسخ سریع، جایگاه رسمی، سرفصل، پیش‌نیاز، مسیر یادگیری، خطاهای رایج، CTA و منبع تکمیل شدند.
- صحت ۱۴۰۶: عنوان‌های رسمی و ضرایب مستقیماً با اطلاعیه و جدول سازمان سنجش تطبیق داده شدند؛ هر دو ماتریس کد ضریب ۱ و ۲ مجموعهٔ ۱۲۰۹ ثبت شد و تفاوت عنوان یکپارچهٔ «داده‌ساختارها و الگوریتم‌ها» در ۱۲۷۶/۱۲۷۷ با دو عنوان جدا اما یک گروه مشترک در ۱۲۰۹ حفظ شد.
- منابع: اطلاعیهٔ رسمی سنجش، سیزده صفحهٔ درس Syllab، دو برنامهٔ درسی مرجع و دو مسیر متعلق به `@konkurcom` به‌صورت دادهٔ مستقل از UI ثبت شدند؛ منبع یا نتیجهٔ ساختگی اضافه نشد.
- یکپارچگی: صفحهٔ درس در حالت API و fallback، لایهٔ تحریریهٔ یکسان را حفظ می‌کند؛ فیلتر `/resources?subject=...` نیز واقعاً اعمال و حالت خالی صادقانه نمایش داده می‌شود.
- SEO و کشف: هر ۲۰ مسیر SSR دارای status ۲۰۰، canonical دقیق، یک `h1`، منبع و تاریخ بررسی است؛ sitemap درس‌ها URL یکتا و `lastmod` تاریخ ۱۴۰۵/۰۶/۱۷ دارد و محتوای مسیر یادگیری در جست‌وجوی فارسی fallback پیدا می‌شود.
- آزمون‌ها: corpus سه راهنما/هفده هاب و دو ماتریس ضریب PASS؛ smoke هر ۲۰ مسیر و sitemap PASS؛ typecheck، lint، test و build کل workspace PASS و Next.js هر ۸۰ مسیر را ساخت. چهارده Draft تولیدی `article.v2` نیز اعتبارسنجی شدند.
- نمایش: آزمون headless در viewport موبایل ۳۹۰×۸۴۴ برای راهنمای ستون و هاب درس، RTL، فونت IRANYekan و نبود overflow افقی را تأیید کرد.
- بازبینی مستقل: یافتهٔ P0/P1 وجود نداشت؛ دو یافتهٔ P2 دربارهٔ فیلتر منابع و پوشش تست آن اصلاح و دوباره آزموده شد.
- محدوده: migration، CSS عمومی و پنل ادمین تغییر نکرد؛ فاز ۱۲ آغاز نشد و Codebase Memory در هیچ بخش فاز ۱۱ استفاده نشد.

## فاز ۱۲ — ۲۲ راهنمای برنامه‌ریزی و اطلاعات رسمی

- [x] `P12-01` راهنمای شروع از صفر.
- [x] `P12-02` برنامهٔ دوازده‌ماهه.
- [x] `P12-03` برنامهٔ شش‌ماهه.
- [x] `P12-04` برنامهٔ سه‌ماهه.
- [x] `P12-05` برنامه برای داوطلب شاغل.
- [x] `P12-06` راهنمای تغییر رشته به کامپیوتر.
- [x] `P12-07` راهنمای طراحی هفتهٔ مطالعاتی.
- [x] `P12-08` راهنمای مرور فعال.
- [x] `P12-09` راهنمای دفترچهٔ خطا.
- [x] `P12-10` راهنمای جمع‌بندی ماه آخر.
- [x] `P12-11` راهنمای مدیریت زمان جلسه.
- [x] `P12-12` راهنمای انتخاب منبع بر اساس سطح، زمان و بودجه.
- [x] `P12-13` صفحهٔ تغییرات آزمون ۱۴۰۶.
- [x] `P12-14` صفحهٔ دروس و ضرایب مهندسی کامپیوتر.
- [x] `P12-15` صفحهٔ دروس و ضرایب فناوری اطلاعات.
- [x] `P12-16` صفحهٔ دروس و ضرایب علوم کامپیوتر.
- [x] `P12-17` صفحهٔ ثبت‌نام و تقویم رسمی.
- [x] `P12-18` مرکز اطلاعیه‌ها و اصلاحیه‌ها.
- [x] `P12-19` بودجه‌بندی تاریخی مهندسی کامپیوتر.
- [x] `P12-20` بودجه‌بندی تاریخی فناوری اطلاعات.
- [x] `P12-21` بودجه‌بندی تاریخی علوم کامپیوتر.
- [x] `P12-22` صفحهٔ دفترچه‌ها، کلید اولیه، کلید نهایی و تشخیص اختلاف.
- [x] `P12-GATE` کنترل دفترچه و منابع رسمی؛ نمایش بودجه‌بندی به‌عنوان تاریخچه، نه پیش‌بینی قطعی.

### گزارش Gate فاز ۱۲

- محتوا: ۱۲ راهنمای اجرایی مطالعه و ۱۰ صفحهٔ رسمی/داده‌محور در corpus مستقل از UI ساخته شد؛ هر صفحه پاسخ سریع، حداقل سه بخش، منبع و سه قدم بعدی داخلی دارد.
- صحت رسمی: ضرایب و نام‌های سه مجموعه با اطلاعیهٔ رسمی سنجش تطبیق داده شد؛ تقویم ارشد و دکتری ۱۴۰۶ با اعلام رسمی وزارت علوم ثبت و هشدار کنترل اطلاعیهٔ روز سنجش حفظ شد.
- بودجه‌بندی: سه صفحهٔ تاریخی تا زمان کدگذاری سؤال‌به‌سؤال دفترچه‌های رسمی عمداً بدون ردیف عددی‌اند؛ قرارداد آینده شامل سال، دفترچه، سؤال، درس، فصل، مبحث، کلید و بازبین است.
- کشف: هر ۲۲ صفحه در فهرست راهنماها، جست‌وجوی فارسی fallback، sitemap راهنماها، RSS و `llms.txt` قرار گرفت و canonical یکتا دارد.
- نسخه‌بندی: generator اکنون ۳۶ Draft معتبر `article.v2` و ۱۱ منبع یکتا می‌سازد؛ هر ۳۶ payload با schema قرارداد اعتبارسنجی شد و نسخه‌ها همچنان Draft هستند.
- آزمون‌ها: corpus و smoke هر ۲۲ مسیر PASS؛ typecheck، lint، test و build کل workspace PASS و Next.js هر ۱۰۲ مسیر را ساخت.
- نمایش: بررسی headless در viewport موبایل ۳۹۰×۸۴۴ روی صفحهٔ برنامه و صفحهٔ ضرایب، RTL، IRANYekan، یک `h1`، marker فاز و نبود overflow افقی را تأیید کرد.
- محدوده: بانک سؤال، آزمون‌ساز، CSS عمومی، migration و پنل ادمین تغییر نکرد؛ Codebase Memory استفاده نشد.

## فاز ۱۳ — ۱۸ صفحهٔ تصمیم، مستندات و اعتماد

- [x] `P13-01` مقایسهٔ مهندسی کامپیوتر، فناوری اطلاعات و علوم کامپیوتر.
- [x] `P13-02` گرایش‌های ارشد مهندسی کامپیوتر.
- [x] `P13-03` گرایش‌ها و مسیر شغلی فناوری اطلاعات.
- [x] `P13-04` گرایش‌ها و مسیر پژوهشی علوم کامپیوتر.
- [x] `P13-05` راهنمای خواندن صحیح کارنامه.
- [x] `P13-06` سناریوهای درصد و رتبه با نمایش محدودیت داده.
- [x] `P13-07` روش مقایسهٔ دانشگاه‌ها.
- [x] `P13-08` اصول انتخاب‌رشته و خطاهای رایج.
- [x] `P13-09` مطالعهٔ موردی نتیجهٔ ۱۰۰٪ نظریه.
- [x] `P13-10` مطالعهٔ موردی ۱۰ پاسخ صحیح از ۱۱ سؤال معتبر.
- [x] `P13-11` مطالعهٔ موردی ۹ تست صحیح.
- [x] `P13-12` مطالعهٔ موردی تطبیق ۹ سؤال از ۱۲ سؤال.
- [x] `P13-13` مطالعهٔ موردی نتیجهٔ ۱۶ از ۱۹ و رتبه‌های مستند.
- [x] `P13-14` مطالعهٔ موردی سابقهٔ طراحی سؤال سال ۱۴۰۱.
- [x] `P13-15` صفحهٔ محمد رستمی، سوابق و پایان‌نامه.
- [x] `P13-16` صفحهٔ روش تولید و بازبینی محتوا.
- [x] `P13-17` صفحهٔ سیاست منابع، انتساب و کپی‌رایت.
- [x] `P13-18` صفحهٔ سیاست اصلاح، نسخه‌بندی و گزارش خطا.
- [x] `P13-GATE` همهٔ ادعاها سند داشته باشند و هیچ نتیجه یا نقل‌قول تازه‌ای ساخته نشده باشد.

### گزارش Gate فاز ۱۳

- محتوای تصمیم: هشت راهنمای مقایسهٔ رشته، گرایش، کارنامه، سناریوی رتبه، دانشگاه و انتخاب‌رشته با پاسخ سریع، معیار تصمیم، پیوند داخلی و منبع مشخص آماده شد. ادعاهایی که سند رسمی دقیق ندارند صریحاً «پیش‌نویس بازبینی‌نشده» مانده‌اند.
- مطالعات موردی: شش نمونه فقط با همان عدد و سال ارائه‌شده ساخته شد؛ هر مورد محدودیت تک‌نمونه، نبود تضمین، نبود تأیید مستقل و تعارض منفعت مدرس/مالک را روشن می‌کند.
- حریم خصوصی و حقوق: نام دانشجو بدون رضایت ثبت‌شده در سایت بازنشر نمی‌شود. منابع آرشیوی صرفاً `LINK_ONLY` هستند و حق اقتباس یا استفادهٔ تجاری از مالکیت کانال استنباط نشده است.
- منابع: برای هر پیوند، ادعای پشتیبانی‌شده ثبت شد؛ راهنمای گرایش مهندسی کامپیوتر به برنامهٔ تحصیلات تکمیلی `grad-cse.pdf` متصل است و عنوان/URL پایان‌نامه دقیقاً «مسایل بهینه‌سازی شبکه روی منابع افرازشده» حفظ شد.
- اعتماد: صفحهٔ دربارهٔ محمد رستمی، روش تولید محتوا، سیاست منبع و حق نشر و سیاست اصلاح/نسخه‌بندی تکمیل و در footer، sitemap صفحات و `llms.txt` ثبت شدند. فقط `@konkurcom` کانال رسمی معرفی می‌شود.
- انتشار: چهارده محتوای تحریریه به‌صورت معتبر `article.v2` و با provenance صادقانهٔ `external_ai` وارد seed شدند، اما همگی `DRAFT` هستند؛ تا تأیید واقعی انسان در فهرست، جست‌وجو، sitemap، RSS، `llms.txt` یا fallback عمومی ظاهر نمی‌شوند.
- آزمون‌ها: typecheck، lint، تست واحد/قرارداد، build و smoke فازهای ۱۱، ۱۲ و ۱۳ PASS شدند؛ Next.js هر ۱۰۳ مسیر را ساخت. اجرای E2E وابسته به PostgreSQL در این محیط ممکن نبود چون `DATABASE_URL` و سرویس آزمایشی در دسترس نبود؛ منطق fixture با قرارداد `article.v2` و تست corpus کنترل شد.
- محدوده: migration، بانک سؤال، آزمون‌ساز و CSS عمومی تغییر نکرد؛ پنل ادمین باز نشد و Codebase Memory استفاده نشد.

## فاز ۱۴ — کتابخانهٔ منابع و آرشیو @konkurcom

- [x] `P14-01` تبدیل پست‌های متعلق به کاربر به Resourceهای Draft.
- [x] `P14-02` حفظ لینک پست اصلی و نوع محتوا: ویدئو، جزوه، حل تمرین، تحلیل یا اطلاعیه.
- [x] `P14-03` فیلتر بر اساس مقطع، رشته، درس، مبحث، نوع و تاریخ بررسی.
- [x] `P14-04` ساخت مقایسهٔ بی‌طرف منابع با معیار سطح شروع، پوشش، حجم، نمونه و هزینه.
- [x] `P14-05` اتصال هر منبع به هاب درس و برنامهٔ مرتبط.
- [x] `P14-06` نمایش تنها `@konkurcom` به‌عنوان کانال رسمی؛ لینک‌های قدیمی فقط به‌عنوان مستند اصلی.
- [x] `P14-GATE` نبود فایل غیرمجاز، لینک جعلی یا محتوای بدون provenance.

### گزارش Gate فاز ۱۴

- داده و انتشار: هشت پست آموزشی دارای عنوان و موضوع مشخص به‌صورت `Resource` با وضعیت `DRAFT` آماده شد؛ پست‌های ۴۹۸۰، ۴۹۸۱ و ۴۹۸۶ به‌دلیل نامشخص‌بودن محتوا نگه داشته شدند و هیچ پست نتیجه، کارنامه یا ادعای عملکرد به منبع آموزشی تبدیل نشد.
- حقوق و provenance: همهٔ رکوردها فقط `PUBLIC + EXTERNAL_LINK`، بدون فایل، embed، mirror یا artifact هستند. هر لینک دقیقاً با URL منبع فعالِ `mayLink` تطبیق داده می‌شود؛ لغو مجوز یا آرشیو منبع، URL را فوراً از فهرست، جزئیات و تحویل محتوا حذف می‌کند.
- هویت کانال: فقط `@konkurcom` کانال رسمی معرفی می‌شود. پیوندهای `Konkur_answer` با ناشر «آرشیو پیشین متعلق به محمد رستمی» و نقش `LEGACY_EVIDENCE` نگهداری می‌شوند و حق اقتباس، میزبانی یا استفادهٔ تجاری از آن‌ها استنباط نشده است.
- کشف و انتخاب: API عمومی فیلتر مقطع، رشته، درس، مبحث، نوع و بازهٔ تاریخ بررسی دارد. صفحهٔ منابع همین فیلترها، جدول مقایسهٔ بی‌طرف سطح/پوشش/حجم/نمونه/هزینه و اتصال به هاب درس و راهنمای برنامه را با Componentهای موجود نمایش می‌دهد.
- دادهٔ عمومی: از `metadata` فقط هفت فیلد allowlist‌شدهٔ `catalogProfile` خارج می‌شود؛ provenance، یادداشت خصوصی و کلید ذخیره‌سازی هیچ‌گاه در پاسخ عمومی قرار نمی‌گیرند.
- آزمون‌ها: ۹ تست قرارداد، ۱۵ تست واحد API و همهٔ تست‌های corpus فازهای ۱۱ تا ۱۴ PASS؛ typecheck، lint و build API/Web PASS و Next.js هر ۱۰۳ مسیر را ساخت. E2E جدید فیلتر، seed idempotent و لغو مجوز نوشته و lint/typecheck شد، اما اجرای محلی آن به‌دلیل نبود PostgreSQL/Docker ممکن نبود و در CI دارای PostgreSQL اجرا می‌شود.
- محدوده: schema و migration تغییر نکرد، هیچ CSS عمومی افزوده نشد، پنل ادمین باز نشد و Codebase Memory استفاده نشد.

## فاز ۱۵ — بانک کارنامه و انتخاب دانشگاه

- [x] `P15-01` خروجی عمومی و ناشناس کارنامه‌های دارای رضایت.
- [x] `P15-02` فیلتر سال، رشته، سهمیه، گرایش، دانشگاه و بازهٔ رتبه.
- [x] `P15-03` عدم نمایش آمار تجمیعی برای نمونهٔ کمتر از پنج رکورد.
- [x] `P15-04` نمایش حجم نمونه، سال داده، محدودیت و بازهٔ اطمینان در تخمین‌ها.
- [x] `P15-05` ساخت صفحات دانشگاه و برنامه فقط از منابع رسمی.
- [x] `P15-06` اتصال کارنامه، تخمین رتبه و انتخاب‌رشته به یک مسیر واحد.
- [x] `P15-GATE` تست رضایت، ناشناس‌سازی، عدم نشت provenance و جلوگیری از دقت کاذب.

### گزارش Gate فاز ۱۵

- کارنامهٔ عمومی: خروجی فقط از رکوردهای `publicConsent=true` ساخته می‌شود و JSONهای رتبه، درصد و پذیرش با allowlist صریح بازسازی می‌شوند؛ شناسه، provenance، نام، پیوند مدرک و کد رشته‌محل تأییدنشده در پاسخ عمومی جایی ندارند.
- تحلیل بدون دقت کاذب: حجم نمونه و سال‌های داده همیشه نمایش داده می‌شود؛ آمار تجمیعی زیر پنج رکورد عمداً `null` است و در نمونهٔ کافی، میانه، صدک‌های تجربی و توضیح روشن «عدم تضمین» ارائه می‌شود. نرخ پذیرش نیز زیر پنج نمونه پنهان و در نمونهٔ کافی با بازهٔ ویلسون ۹۵٪ همراه است.
- فیلتر و مسیر تصمیم: بانک کارنامه فیلتر سال، مقطع، رشته، سهمیه، رشته‌محل، دانشگاه و بازهٔ رتبه دارد. چهار گام کارنامه ← تخمین بازه‌ای ← رشته‌محل رسمی ← چینش انتخاب‌ها در تمام صفحات مرتبط به هم وصل شد.
- منبع رسمی: دانشگاه، رشته‌محل و ظرفیت فقط با `ContentSource` فعال از نوع `PRIMARY_OFFICIAL` و مجوز پیوند عمومی منتشر می‌شوند. صفحهٔ SSR هر دانشگاه و رشته‌محل، تاریخ بررسی، منبع، ظرفیت تاریخی و هشدار دفترچه/اصلاحیه را نشان می‌دهد؛ رکوردهای legacy بدون منبع حذف نشده‌اند اما عمومی نیستند.
- migration و SEO: migration افزایشی با سه رابطهٔ nullable، کلید خارجی `RESTRICT` و down دارای guard داده ساخته شد. sitemap هشتم برای دانشگاه‌ها و رشته‌محل‌های رسمی، robots، `llms.txt`، canonical، noindex فیلترها و JSON-LD تکمیل شدند.
- آزمون‌ها: ۹ تست قرارداد، ۱۸ تست واحد API، تست‌های corpus وب و smoke واقعی SEO PASS؛ typecheck و lint کل API/Web، Prisma validate، build API/Web و `git diff --check` موفق بودند و Next.js هر ۱۰۴ مسیر را ساخت. تست E2E فاز ۱۵ برای رضایت، ناشناس‌سازی، حداقل نمونه، همهٔ فیلترها و منبع رسمی نوشته و lint/typecheck شد؛ اجرای محلی آن به‌دلیل نبود PostgreSQL/Docker ممکن نبود و CI اکنون migration up → down → up و تمام E2Eها را روی PostgreSQL 16 اجرا می‌کند.
- محدوده: هیچ دادهٔ دانشگاه، ظرفیت، رتبه یا قبولی ساختگی وارد محصول نشد، هیچ CSS عمومی تغییر نکرد، پنل ادمین باز نشد و Codebase Memory استفاده نشد.

## فاز ۱۶ — اتصال محتوا به برنامهٔ شخصی

- [x] `P16-01` استفاده از مقطع، رشته، زمان آزاد و سطح خوداظهاری در شروع مسیر.
- [x] `P16-02` نمایش سه اقدام واقع‌بینانهٔ امروز همراه با دلیل پیشنهاد.
- [x] `P16-03` امکان ذخیره، ادامهٔ مطالعه و افزودن منبع به برنامه.
- [x] `P16-04` اتصال پیش‌نیازها و اهمیت آزمون به بازبرنامه‌ریزی.
- [x] `P16-05` جداسازی صریح «پیشرفت مطالعه» از «تسلط علمی».
- [x] `P16-06` نگهداری سابقهٔ مطالعه و برنامه بدون انقضای سال آزمون.
- [x] `P16-GATE` هیچ دادهٔ ساختگی یا ادعای تسلط بدون شواهد آزمون نمایش داده نشود.

### گزارش Gate فاز ۱۶

- شروع شخصی: هدف اکنون مقطع، رشته، ساعت آزاد هفتگی و سطح خوداظهاری را می‌گیرد. سطح خوداظهاری فقط ترتیب شروع را تنظیم می‌کند و صریحاً به‌عنوان شاهد تسلط استفاده نمی‌شود.
- برنامهٔ واقعی: موتور قطعی برنامه از شواهد واقعی آزمون، درس و منبع منتشرشده و قابل‌دسترسی، پیش‌نیازهای taxonomy و فقط اهمیت آزمونی عددیِ ثبت‌شده در metadata استفاده می‌کند. در نبود سیگنال معتبر، empty state صادقانه نشان داده می‌شود و متن یا دادهٔ ساختگی تولید نمی‌شود. هر روز حداکثر سه اقدام با زمان تخمینی، دلیل و لینک مقصد دارد. جایگزینی برنامه و بازتوزیع کارهای عقب‌افتاده با قفل کاربر و تراکنش اتمیک انجام می‌شود تا درخواست‌های هم‌زمان برنامهٔ دوگانه یا نیمه‌کاره نسازند.
- ذخیره و ادامه: منبع منتشرشده قابل ذخیره/حذف و، پس از کنترل دسترسی و taxonomy، قابل افزودن به برنامه است. منبع تکمیل‌شده دوباره به‌صورت خودکار پیشنهاد نمی‌شود، اما افزودن صریح کاربر آن را برای مطالعهٔ دوباره باز می‌کند. آخرین درس مشاهده‌شده، شمار واقعی بازدید و تکمیل و لینک ادامهٔ دوره از API فعالیت مطالعه خوانده می‌شود.
- مرز سنجش: API و رابط حساب، `LESSON_ACTIVITY` را از `ASSESSMENT_EVIDENCE` جدا نمایش می‌دهند؛ تکمیل درس یا ثبت دقیقه هیچ امتیاز تسلطی نمی‌سازد و تسلط فقط از پاسخ‌های ارزیابی‌شده محاسبه می‌شود.
- تاریخچهٔ پایدار: هر Plan یک snapshot تغییرناپذیر از هدف همان زمان دارد و جلسه‌های مطالعه و همهٔ نسخه‌های برنامه بدون فیلتر سال آزمون با cursor مستقل صفحه‌بندی می‌شوند؛ رابط حساب نیز امکان بارگذاری تدریجی رکوردهای قدیمی‌تر را دارد. migration افزایشی up/down با guard حذف داده و backfill کامل snapshot ساخته شد.
- آزمون‌ها: ۹ تست قرارداد، ۲۳ تست واحد API و همهٔ تست‌های corpus وب PASS؛ typecheck و lint API/Web، Prisma validate، build API/Web و `git diff --check` موفق بودند و Next.js هر ۱۰۴ مسیر را ساخت. سناریوهای E2E جدید برای شروع سرد، سه دلیل واقعی، پیش‌نیاز/اهمیت، جداسازی پیشرفت/تسلط، snapshot، ذخیره و افزودن منبع، rollback و رقابت replan، منبع تکمیل‌شده، تکمیل کار عقب‌افتاده و پیمایش کامل تاریخچه نوشته و typecheck/lint شد؛ اجرای محلی به‌دلیل نبود PostgreSQL/Docker ممکن نبود. CI اکنون migration فاز ۱۶ را روی fixture دارای Goal، Plan و StudySession قدیمی اعمال و backfill سطح `UNKNOWN`، snapshot و منشأ `UNCLASSIFIED_LEGACY` را کنترل می‌کند؛ سپس فازهای ۱۶ و ۱۵ را down و دوباره deploy کرده و تمام E2Eها را روی PostgreSQL 16 اجرا خواهد کرد.
- محدوده: هیچ محتوای آموزشی، شاهد تسلط یا اهمیت آزمونی جعلی اضافه نشد، هیچ CSS عمومی تغییر نکرد، پنل ادمین باز نشد و Codebase Memory استفاده نشد.

## فاز ۱۷ — توسعهٔ دکتری

- [x] `P17-01` راهنمای ستون دکتری مهندسی و علم کامپیوتر.
- [x] `P17-02` راهنمای ستون دکتری فناوری اطلاعات.
- [x] `P17-03` راهنمای ستون دکتری علوم کامپیوتر و بیوانفورماتیک.
- [x] `P17-04` صفحهٔ مواد، ضرایب و تغییرات ۱۴۰۶ دکتری مهندسی و علم کامپیوتر.
- [x] `P17-05` صفحهٔ مواد، ضرایب و تغییرات ۱۴۰۶ دکتری فناوری اطلاعات.
- [x] `P17-06` صفحهٔ مواد و ترکیب‌های ۱۴۰۶ دکتری علوم کامپیوتر و بیوانفورماتیک.
- [x] `P17-07` راهنمای تخصصی معماری سیستم‌های کامپیوتری.
- [x] `P17-08` راهنمای تخصصی نرم‌افزار و الگوریتم.
- [x] `P17-09` راهنمای تخصصی هوش مصنوعی.
- [x] `P17-10` راهنمای تخصصی شبکه و رایانش امن.
- [x] `P17-11` راهنمای تخصصی فناوری اطلاعات.
- [x] `P17-12` راهنمای تخصصی علوم کامپیوتر، بیوانفورماتیک و مسیر پژوهشی.
- [x] `P17-13` راهنمای برنامه‌ریزی آزمون کتبی دکتری.
- [x] `P17-14` راهنمای زبان دکتری.
- [x] `P17-15` راهنمای رزومهٔ پژوهشی.
- [x] `P17-16` راهنمای آمادگی مصاحبه.
- [x] `P17-GATE` تطبیق با آخرین دفترچه و جداسازی روشن آزمون کتبی از مصاحبه.

### گزارش Gate فاز ۱۷

- دامنهٔ محتوا: شانزده راهنمای دکتری با شناسهٔ صریح `P17-01` تا `P17-16`، ترتیب پایدار، پاسخ سریع، حداقل سه بخش، پیوند داخلی و provenance منبع‌به‌منبع آماده شد. سه صفحهٔ ستون، سه صفحهٔ مواد رسمی، شش راهنمای تخصصی و چهار راهنمای آمادگی، کل TODO این فاز را پوشش می‌دهند.
- تطبیق رسمی: جدول‌های رسمی ارائه‌شده توسط کاربر سه مجموعه را جدا می‌کنند: ۲۳۵۴ «مهندسی و علم کامپیوتر»، ۲۳۵۸ «مهندسی فناوری اطلاعات» و ۲۲۴۷ «علوم کامپیوتر و بیوانفورماتیک». در ۲۲۴۷، علوم کامپیوتر ترکیب گروه‌های ۱+۲+۳ و بیوانفورماتیک ترکیب ۱+۴ دارد؛ برای این کد هیچ ضریب یا تعداد سؤالِ نمایش‌داده‌نشده استنتاج نشد. صفحات زمان‌حساس تا بازبینی انسانی و کنترل اصلاحیه‌ها Draft می‌مانند.
- اصلاح محتوای قدیمی: سه fallback قدیمی دکتری از مسیر عمومی حذف و فقط با guard نسخه‌ای ارتقا داده می‌شوند؛ به‌ویژه علوم کامپیوتر دیگر به ۲۳۵۴ نگاشت نمی‌شود، «مهندسی اطلاعات/خدمات IT» در جدول ۲۳۵۸ نیست و امنیت شبکه نیز درس مستقل اعلام نشده است. منبع سنجش، تاریخ بررسی و ادعای پشتیبانی‌شده در provenance ثبت شد.
- مرز پذیرش: صفحات ۰۴ تا ۱۴ صریحاً `WRITTEN` و صفحات رزومه و مصاحبه `POST_WRITTEN` هستند؛ سه راهنمای ستون `BOTH` است اما دو مسیر را در بخش‌های جدا توضیح می‌دهد. هیچ تعداد سؤال، بودجه‌بندی، امتیاز رزومه یا پرسش قطعی مصاحبه ساخته نشده است.
- انتشار و مهاجرت امن: هر شانزده صفحه به `article.v2` با `producer_type=external_ai` و `review_status=draft` تبدیل می‌شود و پیش از بازبینی واقعی انسان در fallback، جست‌وجو، sitemap، RSS یا `llms.txt` ظاهر نمی‌شود. seed با advisory/row lock فقط snapshotهای دقیق و دست‌نخورده را ارتقا می‌دهد؛ نسخهٔ ۱ حفظ می‌شود و ویرایش یا وضعیت انسانی دست‌نخورده می‌ماند.
- عملیات و برگشت: schema و migration پایگاه تغییر نکرد. runbook فرمان بازتولید/اعتبارسنجی، الزام backup پیش از seed و rollback واقعی از راه restore نسخهٔ پشتیبان را ثبت می‌کند؛ برای Draft قدیمی ادعای rollback غیرقابل‌اجرا از API وجود ندارد.
- آزمون‌ها: generator با `--check` همگامی کامل corpus و fixture را می‌سنجد؛ تست‌ها متن، SEO، source claim، سه کد، ترکیب‌های ۲۲۴۷، لینک‌های داخلی قابل‌کلیک و عدم بازنویسی کار انسانی را پوشش می‌دهند. unit/typecheck/lint/build محلی PASS است و E2E کامل PostgreSQL شرط merge باقی می‌ماند.
- محدوده: Gate فاز ۱۷ بسته شد؛ CSS عمومی، schema، بانک سؤال و آزمون‌ساز تغییر نکرد و Codebase Memory استفاده نشد.

## فاز ۱۸ — پولیش، انتشار و سنجش

- [ ] `P18-01` اجرای `typecheck` برای workspaceهای مرتبط.
- [ ] `P18-02` اجرای `lint` برای workspaceهای مرتبط.
- [ ] `P18-03` اجرای `test` و `test:e2e` برای workspaceهای مرتبط.
- [ ] `P18-04` اجرای `build` برای workspaceهای مرتبط.
- [ ] `P18-05` اجرای migration up/down روی پایگاه آزمایشی و بازیابی داده.
- [ ] `P18-06` بررسی canonicalها، sitemapها، structured data، RSS، 404 و redirect.
- [ ] `P18-07` بررسی دستی موبایل، دسکتاپ، RTL، اعداد فارسی، keyboard و screen reader.
- لغوشده — `P18-08` اجرای `/impeccable critique`؛ به درخواست صریح مالک پروژه اجرا نمی‌شود.
- لغوشده — `P18-09` اجرای `/impeccable audit`؛ به درخواست صریح مالک پروژه اجرا نمی‌شود.
- لغوشده — `P18-10` اجرای `/impeccable polish`؛ به درخواست صریح مالک پروژه اجرا نمی‌شود.
- لغوشده — `P18-11` اجرای `/impeccable typeset`؛ به درخواست صریح مالک پروژه اجرا نمی‌شود.
- [ ] `P18-12` اجرای `git diff --check`.
- [ ] `P18-13` ثبت Search Console، Bing Webmaster Tools و IndexNow پس از استقرار دامنه.
- [ ] `P18-14` commit و push نهایی به `origin/main`.
- [ ] `P18-GATE` گزارش فایل‌ها، تست‌ها، صفحات منتشرشده و بدهی‌های باقی‌مانده.

- یادداشت `P18-05`: CI مسیر down/up هر چهار migration دارای `down.sql` را به‌ترتیب معکوس اجرا می‌کند و سپس تمام migrationها را دوباره اعمال می‌کند؛ تیک فقط پس از PASS شدن run نهایی زده می‌شود.
- یادداشت `P18-06`: CI علاوه بر تست‌های corpus، سرور production وب و API واقعی را بالا می‌آورد و canonical، sitemap، structured data، RSS و SSR را می‌سنجد؛ fixture مستقل نیز hard 404، redirect دائمی و خطای upstream را کنترل می‌کند.
- یادداشت `P18-13`: این خط تا زمان وجود مدرک ثبت در حساب‌های Search Console و Bing تیک نمی‌خورد؛ آماده‌سازی فنی یا ارسال IndexNow به‌تنهایی معادل ثبت دو سرویس نیست.

## قابلیت آینده — بانک سؤال و آزمون‌ساز

- [ ] `FUTURE-Q01` تعریف taxonomy و provenance بانک سؤال پس از تکمیل فازهای فعال.
- [ ] `FUTURE-Q02` طراحی آزمون‌ساز شخصی و چرخهٔ سنجش تسلط در برنامه‌ای مستقل.
- [ ] `FUTURE-Q03` ساخت بخش حل تمرین‌های منتخب کتاب مرجع هر درس، فقط با ثبت منبع و مجوز استفاده یا ارجاع حقوقی امن.
- [ ] `FUTURE-Q04` تطبیق هر سؤال با درس، فصل، مبحث و پیش‌نیاز برای تحلیل پوشش و اتصال آینده به برنامه و آزمون‌ساز.
